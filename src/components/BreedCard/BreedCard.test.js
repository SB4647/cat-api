import React from "react";
import { desribe, test, it } from "jest";
import { BreedCard } from "./BreedCard";

describe("BeedCard tests", () => {
  let component;

  beforeEach(() => {
    // component =
  });
});
